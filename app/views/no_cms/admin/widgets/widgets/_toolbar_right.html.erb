<%= f.submit t('.submit'), name: :submit, class: "btn right" %>
<% # We must check that routes are mounted. Otherwise it will get a 404 error %>
<%= link_to t('.visit'), no_cms_widgets.url_for(@widget), target: '_blank', class: "preview btn right" if defined?(no_cms_widgets) && !@widget.new_record?  %>
